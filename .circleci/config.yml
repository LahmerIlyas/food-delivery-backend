version: 2.1
jobs:
  build-test:
    docker:
      # specify the version you desire here
      - image: circleci/node:14.0.0

    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  build-test:
    jobs:
      - build-test